mod craindown;
mod error;
mod lex;
mod parse;
mod util;

fn main() {
    // Create a preprocessing step for eliminating starting and ending whitespace in
    // a file? This would actually break the ability to accurately report errors...
    println!("Hello, world!");
}
